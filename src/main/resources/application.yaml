spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/orclpdb
    username: system
    password: oracle
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
HMAC_SECRET: 123